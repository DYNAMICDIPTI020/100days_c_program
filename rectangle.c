#include"stdio.h"
int main()
{
	int area , height , width;
	printf("Enter the height ");
	scanf("%d", &height);	
	printf("Enter the width ");
	scanf("%d", &width);
	area  = height * width ;
	printf("the area of the rectagnle is %d", area);
	
}
